apiVersion: v1
kind: Service
metadata:
  name: godemode-backend
  namespace: godemode
  labels:
    app: godemode-backend
    component: backend
spec:
  type: ClusterIP
  selector:
    app: godemode-backend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
